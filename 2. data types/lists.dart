void main() {
  // List<int> numbers = [1, 2, 3, 4];
  var giveMeFive = true;
  var numbers = [
    1,
    2,
    3,
    4,
    // collection if
    if (giveMeFive) 5, // giveMeFive가 true이면 5 추가
  ];
  print(numbers);
  // numbers.add(1);
}
